version: "3.0"

services:
 faas-gateway:
   image: faas-scaffold_faas-gateway:latest
   build:
     context: ./faas-gateway
   ports:
     - "8080:8080"
   volumes:
     -  /var/run/docker.sock:/var/run/docker.sock
   networks:
     - faas
 multiply:
   image: faas-scaffold_multiply:latest
   build:
     context: ./multiply
   labels:
     - "faas.name=multiply"
     - "faas.port=8080"
   networks:
     - faas

networks:
  faas: